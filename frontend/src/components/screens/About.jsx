import React from "react";
import { Typography } from "@material-ui/core";

export default () => (
  <>
    <Typography variant="h4">About</Typography>
    <Typography variant="h5">IVJ's Bufunfa</Typography>
    <Typography variant="body1">
      This is a portfolio project used to test different technologies. Not too
      complex that nobody can understand, not too simple that we have only hello
      worlds.
    </Typography>
  </>
);
